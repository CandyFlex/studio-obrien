# Stage 0 — Candidate: Lincolnton retrofit + boost

**Target query:** `web design lincolnton nc`
**Asset:** existing page https://studioobrien.com/lincolnton-nc-web-design (v2 template, live)
**Type:** retrofit + internal-link boost of an existing geo page — NOT a new article.
**Source of candidacy:** first Apify SERP snapshot (2026-07-20, run 1) put us at
**#13** — our first confirmed organic rank anywhere. SEO-STRATEGY cycle queue #1
("cheapest win": page 2 → page 1 is the shortest distance in the portfolio).

**Why this target (doctrine fit):**
- Explicit-geo long-tail = the terrain where low authority matters least.
- Page already exists, indexed, on the v2 template; treatment is boost
  (internal links, entry points, distribution), not creation.
- Per C2 playbook: page-2 assets get retrofit + internal-link boost + 1-2 real
  links before any new content ships.

**Caution recorded at intake (2026-07-20):** two later same-day re-runs did NOT
find us in top ~30 for this term. The #13 is a flicker, not a stable rank —
see 01-serp.md. Treatment unchanged (boost is exactly what stabilizes a
flickering page), but do not report "we rank #13" as a stable fact.
